syntax = "proto3";

message BasicRequestInfo {
    string type = 1;    // sync/async
    bool use_sandbox = 2;   // true/false 
    string request_topic = 3; // topic of request
    string response_topic = 4;  // topic to response
}